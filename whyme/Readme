I have used an Oracle database. So, In the Databaseutil class change the classname and driver information. 
Also, FileServerDAO class uses queries which are compatible with oracle.
access the 'query' file to create table , The commands are already written in the file.
just execute the queries
